var removeNegativeValue = function(values) {
  // Your future job begins here ...
    var output= []
    return output;


    var input = [34, -5, 9, 18, 0, -7, 32];
    const result = input.filter(input => input > -1);

    return result;
}

module.exports = {
  title: 'Exercise 1',
  run: removeNegativeValue
}
